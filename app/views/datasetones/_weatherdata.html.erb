	</div>
	<div class="span4">
  <h2>Weatherdata</h2>
  <h5><small>Last updated one second ago </small></h5>
  <table>
  <th></th>
  <th>Temperature</th>
  <th>Dewpoint</th>
  <th>Airpressure</th>
  <th>Stationpressure</th>
  <th>Visibility</th>
  <th>Windspeed</th>
  <th>Precipitation</th>
  <th>Snowlevel</th>
  <th>Cloudcoverage</th>
  <th>Winddirection</th>
  <tr>
    <td><strong>Date<%= @measurement.date %></strong></td>
    <td><%= @measurement.temp.round(1) %></td>
    <td><%= @measurement.dewp.round(1) %></td>
    <td><%= @measurement.stp.round(1) %></td>
    <td><%= @measurement.slp.round(1) %></td>
    <td><%= @measurement.visib.round(1) %></td>
    <td><%= @measurement.wdsp.round(1) %></td>
    <td><%= @measurement.prcp.round(1) %></td>
    <td><%= @measurement.sndp.round(1) %></td>
    <td><%= @measurement.cldc.round(1) %></td>
    <td><%= @measurement.wnddir %></td>
  </tr>
  </table>
  <h2> Nearby stations </h2>
  <% if @station.nearbys(25).any? %>
	  <% for s in @station.nearbys(25) %>
	  	<li><%= link_to "#{s.country.capitalize}, #{s.city.capitalize}", s %> (<%= (Geocoder::Calculations.distance_between(@station.coordinates, s.coordinates).round(2)) %>km) </li>
	  <% end %>
	<% else %>
		<p> No stations found in a radius of 25km. </p>
	<% end %>